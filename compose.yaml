services:
  hayasend:
    build:
      context: .
      args:
        VERSION: local
    ports:
      - "127.0.0.1:8787:8787"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    init: true
